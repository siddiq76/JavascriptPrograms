let bubblesort = (arr) => {
    let n = arr.length;
    for(let i = 0; i < n - 1; i++){
        for(let j = 0; j <= n - i; j++){
            //swap the numbers
            if(arr[j] > arr [j+1]){
                [arr[j], arr[j+1]]]
            }
        }
    }
}